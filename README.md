# VPN Router для Raspberry Pi с веб-интерфейсом

Полное решение для создания VPN роутера на базе Raspberry Pi с удобным веб-интерфейсом для управления.

## 🎯 Возможности

- ✅ Автоматическая настройка Xray (VLESS + Reality)
- ✅ Wi-Fi точка доступа (Hotspot)
- ✅ Прозрачное проксирование всего трафика
- ✅ Веб-интерфейс для управления настройками
- ✅ Мониторинг статуса в реальном времени
- ✅ Автоматический перезапуск при сбоях
- ✅ Защита от случайного отключения

## 📦 Что входит в комплект

1. **Основной скрипт установки** - настраивает VPN роутер с нуля
2. **Веб-интерфейс** - управление через браузер
3. **Systemd сервисы** - автозапуск при включении

## 🚀 Быстрый старт

### Вариант 1: Полная установка (рекомендуется)

```bash
# Скачайте обновленный скрипт установки
wget https://your-server.com/vpn-router-setup.sh

# Запустите установку
sudo bash vpn-router-setup.sh
```

Скрипт запросит:
1. VLESS URL (ссылку от вашего VPN провайдера)
2. Название Wi-Fi сети (по умолчанию: DarkStarVPN)
3. Пароль Wi-Fi (по умолчанию: SuperSecretPass)

### Вариант 2: Добавление веб-интерфейса к существующей установке

Если у вас уже установлен базовый VPN роутер:

```bash
# Скачайте установщик веб-интерфейса
wget https://your-server.com/install-webui.sh

# Запустите установку
sudo bash install-webui.sh
```

## 🌐 Использование веб-интерфейса

### Доступ к интерфейсу

После установки подключитесь к Wi-Fi сети вашего роутера и откройте в браузере:

```
http://10.42.0.1:8888
```

### Возможности интерфейса

#### 📊 Панель статуса
- **Hotspot** - статус Wi-Fi точки доступа
- **VPN** - статус Xray сервиса
- **Клиенты** - количество подключенных устройств

#### ⚙️ Настройки
- Изменение названия сети (SSID)
- Изменение пароля Wi-Fi
- Применение настроек с таймером на переподключение

### Изменение настроек Wi-Fi

1. Откройте веб-интерфейс
2. Введите новое название сети и/или пароль
3. Нажмите "Применить настройки"
4. Дождитесь окончания 10-секундного таймера
5. Переподключитесь к новой Wi-Fi сети

**Важно:** После изменения настроек вам потребуется переподключиться к Wi-Fi сети с новыми данными!

## 🔧 Управление системой

### Проверка статуса сервисов

```bash
# Статус Xray VPN
sudo systemctl status xray

# Статус веб-интерфейса
sudo systemctl status vpn-router-web

# Статус всех сервисов
sudo systemctl status xray vpn-router-web redsocks
```

### Просмотр логов

```bash
# Логи Xray
sudo journalctl -u xray -f

# Логи веб-интерфейса
sudo journalctl -u vpn-router-web -f

# Или прямой доступ к файлам логов
sudo tail -f /var/log/xray/error.log
sudo tail -f /var/log/vpn-router/webui.log
```

### Перезапуск сервисов

```bash
# Перезапуск Xray
sudo systemctl restart xray

# Перезапуск веб-интерфейса
sudo systemctl restart vpn-router-web

# Перезапуск hotspot
sudo nmcli con down Hotspot && sudo nmcli con up Hotspot
```

### Ручное изменение настроек

Если веб-интерфейс недоступен, можно изменить настройки вручную:

```bash
# Редактирование конфигурации
sudo nano /etc/vpn-router/config.json

# Применение изменений
sudo nmcli con down Hotspot
sudo nmcli con delete Hotspot
sudo nmcli dev wifi hotspot ifname wlan0 ssid "НовыйSSID" password "НовыйПароль"
sudo nmcli con modify Hotspot connection.autoconnect yes
```

## 🛠️ Устранение неполадок

### Веб-интерфейс не открывается

```bash
# Проверьте статус сервиса
sudo systemctl status vpn-router-web

# Проверьте, слушает ли порт 8888
sudo ss -lntp | grep 8888

# Перезапустите сервис
sudo systemctl restart vpn-router-web
```

### VPN не работает

```bash
# Проверьте статус Xray
sudo systemctl status xray

# Проверьте конфигурацию
sudo /usr/local/bin/xray test -c /usr/local/etc/xray/config.json

# Посмотрите логи ошибок
sudo tail -100 /var/log/xray/error.log
```

### Hotspot не создается

```bash
# Проверьте Wi-Fi интерфейс
ip addr show wlan0

# Проверьте статус NetworkManager
sudo systemctl status NetworkManager

# Попробуйте пересоздать hotspot
sudo nmcli con delete Hotspot
sudo nmcli dev wifi hotspot ifname wlan0 ssid "DarkStarVPN" password "SuperSecretPass"
```

### Клиенты подключаются, но интернет не работает

```bash
# Проверьте IP forwarding
cat /proc/sys/net/ipv4/ip_forward  # Должно быть 1

# Проверьте правила iptables
sudo iptables -t nat -L REDSOCKS -v

# Проверьте статус redsocks
sudo systemctl status redsocks

# Перезапустите все
sudo systemctl restart xray redsocks
```

## 📁 Структура файлов

```
/opt/vpn-router/
├── app.py                          # Веб-приложение

/etc/vpn-router/
├── config.json                     # Конфигурация (SSID, пароль)

/usr/local/etc/xray/
├── config.json                     # Конфигурация Xray

/var/log/vpn-router/
├── webui.log                       # Логи веб-интерфейса
└── webui-error.log                 # Ошибки веб-интерфейса

/var/log/xray/
├── access.log                      # Логи доступа Xray
└── error.log                       # Ошибки Xray

/etc/systemd/system/
├── xray.service                    # Systemd сервис Xray
└── vpn-router-web.service          # Systemd сервис веб-интерфейса
```

## 🔐 Безопасность

### Рекомендации

1. **Измените пароль Wi-Fi** - используйте сложный пароль (минимум 8 символов)
2. **Доступ к веб-интерфейсу** - доступен только из локальной сети (0.0.0.0:8888)
3. **Обновления** - регулярно обновляйте систему:
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

### Ограничение доступа к веб-интерфейсу (опционально)

Если хотите добавить авторизацию:

```bash
# Установите nginx и настройте basic auth
sudo apt install nginx apache2-utils
sudo htpasswd -c /etc/nginx/.htpasswd admin

# Настройте nginx как reverse proxy с авторизацией
```

## 📊 Мониторинг

### Проверка подключенных клиентов

```bash
# Список подключенных устройств
sudo iw dev wlan0 station dump

# Количество клиентов
sudo iw dev wlan0 station dump | grep Station | wc -l
```

### Мониторинг трафика

```bash
# Установка vnstat
sudo apt install vnstat

# Просмотр статистики
vnstat -i wlan0
```

## 🔄 Обновление

### Обновление Xray

```bash
# Скачайте новую версию
cd /tmp
XRAY_VERSION=$(curl -s https://api.github.com/repos/XTLS/Xray-core/releases/latest | jq -r .tag_name)
wget "https://github.com/XTLS/Xray-core/releases/download/${XRAY_VERSION}/Xray-linux-arm64-v8a.zip"

# Остановите сервис
sudo systemctl stop xray

# Замените бинарник
unzip Xray-linux-arm64-v8a.zip
sudo mv xray /usr/local/bin/
sudo chmod +x /usr/local/bin/xray

# Запустите сервис
sudo systemctl start xray
```

### Обновление веб-интерфейса

Просто замените `/opt/vpn-router/app.py` новой версией и перезапустите:

```bash
sudo systemctl restart vpn-router-web
```

## 💡 Дополнительные возможности

### Добавление DNS фильтрации

```bash
# Установка Pi-hole (опционально)
curl -sSL https://install.pi-hole.net | bash
```

### Мониторинг с Grafana

Можно добавить сбор метрик и визуализацию в Grafana.

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи (см. раздел "Просмотр логов")
2. Убедитесь, что все сервисы запущены
3. Проверьте конфигурационные файлы
4. Перезагрузите Raspberry Pi

## 📝 Changelog

### v1.0 (2024-12-23)
- ✅ Первый релиз
- ✅ Базовая установка VPN роутера
- ✅ Веб-интерфейс управления
- ✅ Мониторинг статуса
- ✅ Автоматический перезапуск

---

**Удачного использования! 🚀**
